---
target: homepage (app/page.tsx)
total_score: 26
max_score: 40
na_heuristics: 
p0_count: 1
p1_count: 3
timestamp: 2026-08-23T20-24-37Z
slug: app-page-tsx
---
`Method: dual-agent (A: a8757241a09074034 · B: a444d09a14a769238)`

Browser automation unavailable: no overlay injection, no screenshots, nothing verified in pixels. Evidence is source + byte-exact rendered HTML from the dev server on :3212.

## Design Health Score

| # | Heuristic | Score | Delta | Key Issue |
|---|-----------|-------|---|---|
| 1 | Visibility of System Status | 2 | +1 | Focus ring passes on both grounds now, but 16 invisible elements remain in the tab order (drawer 8, mega-menu 8). Sliders give no position cue. |
| 2 | Match System / Real World | 3 | +0 | "SEO & GEO", "SEO, AEO & PPC", "SEO & AEO" for one capability; neither term defined. |
| 3 | User Control and Freedom | 3 | +1 | useAutoCycle bails under reduced-motion and stops on select (verified). Gap: hover-pause only, no onFocus/onBlur, no visible control. |
| 4 | Consistency and Standards | 2 | +0 | 13 light / 2 dark abandons the ground rhythm. CrossIndustry.tsx hardcodes #1633bd (second accent hue + raw hex). |
| 5 | Error Prevention | 3 | +1 | Audit field validates shape, keeps value, role="alert". All 4 hero proof cards still land on /case-studies. |
| 6 | Recognition Rather Than Recall | 3 | +1 | Industry cards predict destination now. Process still hides step names below sm. |
| 7 | Flexibility and Efficiency | 2 | +0 | Pricing carries id="pricing" and nothing links to it. 15 sections, no accelerator. |
| 8 | Aesthetic and Minimalist Design | 2 | +0 | Three consecutive sections make the same breadth claim. CrossIndustry is 16 undifferentiated blurbs. |
| 9 | Error Recovery | 3 | +1 | One error surface handled properly: negative-ink, aria-invalid, aria-describedby, specific recovery copy. |
| 10 | Help and Documentation | 3 | +0 | Strong FAQ, but after the finalCta band, and it carries "30 days of free support" which appears in no pricing tier. |
| **Total** | | **26/40** | **+5** | **Needs work -> mid-band** |

Heuristic 1 lowered from Assessment A's 3: A scored before B's DOM sweep found 16 focusable-but-invisible elements, which is a failure of exactly that heuristic. All ten applicable.

## Design Specificity Verdict

Authored, roughly 60/40, but the interchangeable 40% holds the most expensive real estate.

What is unmistakably this product is now enforced in code: featuredTestimonial() pulls from testimonials-data.ts so a homepage tab cannot drift from /testimonials; detailSlugs.has() points at a detail page only when one exists and degrades to the client's live site; Testimonials.tsx renders a link-out because rating.score is empty. Three mechanisms, not three intentions.

Category-shaped: Included.tsx (centered heading, 3 tabs, 3 equal cards, unfalsifiable claims), whyZenith's five icon cards, and Process.tsx - a 440px rising curve plotting nothing, no axis, no units, on a page whose first principle is checkability.

Deterministic scan: 53 findings, all design-system-font-size. 1 on-target (down from 5): CaseStudies.tsx:90 text-[1.375rem]. 52 live outside the homepage import graph.

Contrast (computed): #97a3bc/#0a1020 7.47 PASS; #59637a/#fff 6.02 PASS; #59637a/#f4f6fa 5.56 PASS; #fff/#02013a 19.66 PASS; #f6f8fc/#0a1020 17.83 PASS; #c2262b/#fff 5.82 PASS; #a2650a/#fff 4.77 PASS; #1e9e6a/#fff 3.41 FAIL at 4.5:1; #35c88c/#fff 2.14 FAIL.

Focus ring verified: tone-light #02013a gives 19.66 on white / 18.17 on light-surface; tone-dark #f6f8fc gives 17.83 / 16.31 / 14.29 on the three dark grounds. No border-radius applied by the rule. All 6 ground inversions carry the correct tone class; no focus-ring bug found.

## Overall Impression

The accessibility floor went from broken to above category, and then B found a defect class neither pass caught before: elements that are invisible but still focusable. The biggest remaining problems are not defects - the sharpest argument is rendered on the wrong route, and a third of the persuasive copy never reaches the HTML answer engines read.

## What's Working

1. Proof plumbing engineered rather than decorated: three independent mechanisms make content drift structurally impossible.
2. The pricing reassurance line placed under the buttons rather than inside the cards - one line, no new claim, at the only moment the question is live.
3. The migration service card demonstrates migration (BeforeAfterSlider instead of a mockup, client name withheld so it stays a demo).

## Priority Issues

### [P0] Sixteen invisible elements still in the tab order; the previous fix was incomplete
What: mobile drawer (Nav.tsx:236-241) is pointer-events-none opacity-0 with aria-hidden="true" and 8 focusable <a> inside - axe aria-hidden-focus, plus 8 phantom tab stops below 768px. Services mega-menu (Nav.tsx:138-146) is max-h-0 opacity-0 overflow-hidden with 8 more focusable links and no aria-hidden. The onFocus added last round opens the panel but never made its links unreachable when closed; tab order reaches them after the mobile toggle, by which point menuOpen is false.
Why: a keyboard user hits up to 16 stops on things they cannot see, with the now-visible focus ring landing on nothing. Worse than the original bug.
Fix: add `inert` to both subtrees when closed (React 19 supports it natively), or visibility:hidden alongside the opacity transition. inert clears the tab order and the a11y tree in one move.
Command: /impeccable audit

### [P1] A third of the page's persuasive copy never reaches the HTML
What: Process server-renders only the active step - 4 of 5 step headings and all their bullets absent (grep count 0 each). Testimonials renders only items[0] - 4 of 5 quotes and outcome headlines never ship.
Why: PRODUCT.md Principle 2 makes machines a first-class reader, robots.ts courts GPTBot and ClaudeBot, and /services/seo-aeo-ppc sells this capability. The page withholds a third of its evidence from them.
Fix: render all panels and toggle with CSS. Audit.tsx already does this correctly (all 3 tab bodies present) - copy its pattern.
Command: /impeccable harden

### [P1] DESIGN.md states four things the code contradicts
What: (a) "the -ink value clears 4.5:1" - positive-ink #1e9e6a is 3.41:1 on white; red and amber pass, green does not. (b) "exactly one box-shadow in the entire component tree" - Tailwind ring-* compiles to box-shadow: Pricing.tsx:125 (2 rendered), Hero.tsx:80 ring-2 (6 rendered). (c) "Borders are always exactly 1px, no 2px borders" - Process.tsx:178 border-t-2, 5 rendered. (d) "Forms carry an offscreen honeypot field" - the audit form does not.
Why: a design doc that overstates is worse than none; the next reader either "fixes" working code to match a false rule or learns to distrust the file. The -ink claim is live: as written it licenses green text on white at 3.41:1.
Fix: correct all four in DESIGN.md and the sidecar. For -ink, prefer darkening positive-ink until it actually clears 4.5:1 - the rule is right, the value is wrong.
Command: /impeccable document

### [P1] The sharpest argument is authored and rendered on a different route
What: content/home.ts defines `comparison` - 7 criteria x 4 columns, every cell rated. "EUR15k-50k+" and "2-4 months" against "From EUR1,750, fixed" and "3-4 weeks". ComparisonTable is imported by app/case-studies/page.tsx only. The homepage answers "why not a freelancer, why not AI" with whyZenith's five icon cards, one of which reads "Europe based, pragmatically raised".
Why: the primary persona lands cold and price-aware. That table answers their live question, one route away, with nothing signalling it exists.
Fix: render ComparisonTable on the homepage in place of Problem, keeping the two whyZenith cards that carry real claims. Make it one of the navy sections.
Command: /impeccable layout

### [P2] Ground rhythm still not built; four metric-bearing hero cards land on a generic index
What: rendered tone order L L D L L L L L L L D L L L L - 13 white, 2 navy, seven consecutive white sections. All four hero.featured cards use href "/case-studies", including Scottish Luxury Experience ($521k in 7 months) which has a detail page CaseStudies.tsx links to thirty sections later.
Fix: flip Testimonials and CaseStudies to tone="dark". Point each hero card at its slug; Fort Lauderdale has no detail page, so swap it for MOD Digital.
Command: /impeccable layout

## Persona Red Flags

Marta (non-technical owner, cold from an AI answer): "Pavle" appears once in the rendered DOM, inside a quote spoken by someone else. The founder block is authored and rendered nowhere. The Legend Partner badge is not a link; "Wix Legend Partner" exists on this page only inside JSON-LD - visible to crawlers, invisible to her. Trust architecture still inverted.

Dan (agency owner): offer is card 3 of 6, visually identical. White-label band is section 11, below retail tiers that anchor the wrong number. Its one line to him carries no figure, while 10% referral / 3-day feedback / 2 revision rounds are final, published, and unmentioned.

Ray (finance partner comparing quotes): tier names carry no scope. "30 days of free support after launch" is in FAQ #3 and the unrendered comparison table but in no tier feature list. "The Zenith" has no price and a different CTA verb.

## Minor Observations

- The persisted footer-wordmark ignore is keyed on value alone, not file: every future 48px font-size in the tree is now silently suppressed. Should be scoped with --file.
- <figcaption> with no <figure> parent in Testimonials: invalid content model, caption not associated with the quote.
- /services/landing-pages.webp is 779KB, requested from inside the closed nav mega panel.
- 12.9MB of unreferenced PNGs still ship in /public/portfolio-blocky/.
- The mock audit report renders 62 at text-display, the treatment reserved for real metrics, with nothing labelling it an example.
- "The Zenith Sprint" is authored in content/home.ts and never rendered.
- Tenure contradiction: "Wix Professionals since 2021" against "Seven years of cross-industry projects" one scroll apart.
- Both <nav> elements carry aria-label="Primary"; defensible while they are mutually exclusive by viewport, but fragile.
- Clean: zero em dashes rendered, 0 anchors and 0 buttons without accessible names, 0 duplicate IDs, no heading-level skips, one <h1>, skip link with valid target, focus trap and Escape in the dialog.

## Questions to Consider

1. Thirteen white sections, two navy. Is "The Drafting Table" the system this page was built in, or one documented after the fact for a page that had settled into a white stack?
2. Two full-bleed 480px bands whose entire image is a texture JPEG, on a page selling websites, one headlined "The websites behind 100+ business wins". If a prospect screenshots one section, is it one of the two you gave the most pixels to?
3. The page names its process "The Zenith Sprint" in the content file and renders "Step 1...Step 5" over 440px of undated curve. If the process is not worth naming, is it worth a chart that plots nothing?
4. A one-founder agency asking EUR3,750, whose product doc says "One founder, one standard", ships a homepage where the founder's name appears once, inside someone else's quote. Deliberate, or never rendered?
